import { Note } from './types';
export declare function noteToAbsoluteIndex(note: Note): number;
export declare function increaseNotePitch(note: Note): Note;
export declare function decreaseNotePitch(note: Note): Note;
